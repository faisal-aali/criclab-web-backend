#!/usr/bin/env bash
# Reject the push if the app does not compile (same check as GitHub CI).
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

while read -r _local_ref _local_sha _remote_ref _remote_sha; do
  :
done

python3 -m compileall -q app
