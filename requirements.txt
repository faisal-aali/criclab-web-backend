fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pymongo>=4.10.0
motor>=3.6.0
# opencv-contrib-python is pulled in by mediapipe; keep headless out to avoid clashes.
opencv-contrib-python>=4.10.0
# mediapipe pins numpy<2 and requires Python 3.10-3.12 (use the .venv312 environment).
# Solutions API (mp.solutions.pose) was removed in 0.10.30+ — keep below that.
numpy<2
mediapipe>=0.10.14,<0.10.30
matplotlib>=3.8.0
cloudinary>=1.40.0
reportlab>=4.2.0
httpx>=0.27.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
pillow>=11.0.0
# --- Auth, email, validation (added for the accounts platform) ---
pyjwt>=2.9.0
bcrypt>=4.2.0
aiosmtplib>=3.0.0
email-validator>=2.2.0
